// Compat re-export: `_actions/ops.ts` still imports the routing helpers from
// this pre-move path. Remove once that import points at inquiries/_components.
export * from '../../inquiries/_components/routing'
