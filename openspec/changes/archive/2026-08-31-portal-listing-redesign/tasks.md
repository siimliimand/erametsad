## 1. Kõik objektid tab

- [x] 1.1 Add the `koik` tab to `LISTING_TABS` as the first entry backed by an explicit `allTypes: true` flag on `ListingTabDef` (empty `objectTypes` keeps meaning "no schema value yet"), label "Kõik objektid", heading "Aktiivsed oksjonid"; make it `DEFAULT_LISTING_TAB` in `resolveListingTab`; extend `statsForTab`/`_lib/summary` so `allTypes` sums every objectType bucket for count, area, volume, and min bid <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/src/app/(portal)/_components/ListingTabs.tsx, apps/platform/src/app/(portal)/_lib/summary.ts] -->
- [x] 1.2 In the listing page replace the `hasTypes` gate with `tabDef.allTypes || tabDef.objectTypes.length > 0` so Kõik queries and renders while Põllumaad keeps its empty state; include Kõik in the tab-count map and `generateMetadata` heading <!-- agent: fullstack-engineer.build, depends_on: [1.1], touches: [apps/platform/src/app/(portal)/page.tsx] -->
- [x] 1.3 Tests: unknown/missing `tab` resolves to Kõik, Kõik count equals the sum of all type buckets, Põllumaad still renders its empty state, shareable `?tab=koik` URL round-trips <!-- agent: fullstack-engineer.build, depends_on: [1.2], touches: [apps/platform/src/app/(portal)/_components/__tests__/**] -->

## 2. Listing layout

- [x] 2.1 Restructure the page into the 12-column layout at `lg` (filters aside `lg:col-span-3`, results main `lg:col-span-9`); remove the Loendivaade/Kaardivaade toggle and the `view` branch (keep parsing `view=kart` as inert); always run `listAuctionMapPoints` alongside `listAuctions` via the existing `Promise.all` and render `ListingMap` above the results <!-- agent: fullstack-engineer.build, depends_on: [1.2], touches: [apps/platform/src/app/(portal)/page.tsx] -->
- [x] 2.2 Add the results bar component ("Leitud N oksjonit" + Sorteeri select posting the same `sort`/`order` params, Estonian pluralization for N) above the card grid, below the map <!-- agent: fullstack-engineer.build, depends_on: [2.1], touches: [apps/platform/src/app/(portal)/_components/ListingResultsBar.tsx, apps/platform/src/app/(portal)/page.tsx] -->
- [x] 2.3 Convert `ListingFilters` to the sidebar presentation: drop its Sorteeri select, keep county/parish cascade, species and logging chips, area/volume/price ranges, active-count badge, Tühjenda, and Telli teavitus; collapse the whole panel behind a disclosure below `lg` <!-- agent: fullstack-engineer.build, depends_on: [2.2], touches: [apps/platform/src/app/(portal)/_components/ListingFilters.tsx] -->
- [x] 2.4 Make `ListingMap` responsive-height (about 400px at desktop, about 240px below `lg`) for its always-visible slot above the results <!-- agent: fullstack-engineer.build, depends_on: [2.1], touches: [apps/platform/src/app/(portal)/_components/ListingMap.tsx] -->
- [x] 2.5 Tests: results-bar count text and sort param round-trip, filters disclosure below `lg`, sidebar placement at `lg`, map renders without the view toggle, legacy `?view=kart` renders the standard layout <!-- agent: fullstack-engineer.build, depends_on: [2.3, 2.4], touches: [apps/platform/src/app/(portal)/_components/__tests__/**] -->

## 3. Lot card v2

- [x] 3.1 Extend `LotCard` with optional presentation props (`typeLabel`, `parish`, `speciesNames`, `volumeM3`, `ctaLabel` default "Vaata lähemalt"): image overlays for the type badge and countdown, 2×2 Lucide metadata grid (`MapPin` parish+county, `Ruler` ha, `Trees` species, `Package` m³) with empty cells collapsing, Alghind block, CTA inside the existing whole-card link; without the new props render the current minimal card so `AuctionTicker` and `ArchiveCard` are untouched <!-- agent: fullstack-engineer.build, depends_on: [], touches: [packages/ui/src/components/content/LotCard.tsx] -->
- [x] 3.2 Extract the species code→name table from `ListingFilters` into a shared helper (for example `(portal)/_lib/species.ts` with `speciesNames(codes)`) and use it for both the filter chips and the card <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/src/app/(portal)/_lib/species.ts, apps/platform/src/app/(portal)/_components/ListingFilters.tsx] -->
- [x] 3.3 Map the new fields in `LiveListing.lotCardProps` from `AuctionSummary` (objectType→Estonian type label, parish+county, species names, volume m³) and change the grid to 2 columns at `sm`+ inside the narrower main column <!-- agent: fullstack-engineer.build, depends_on: [3.1, 3.2], touches: [apps/platform/src/app/(portal)/_components/LiveListing.tsx] -->
- [x] 3.4 LotCard tests: full presentation with all props, minimal presentation for legacy consumers, empty-cell collapse, CTA inside the single card link <!-- agent: fullstack-engineer.build, depends_on: [3.1], touches: [packages/ui/src/components/content/__tests__/**] -->

## 4. Visual identity

- [x] 4.1 Update `tokens.css` values: primary `#012d1d`, primary-hover `#1b4332`, primary-light `#c1ecd4`, ink `#181a2e`, ink-muted `#414844`, bg-page `#fbf8ff`, bg-mist `#f4f2ff`, border `#c1c8c2`; radii card 8px, input 8px, button pill, hero 12px; container-max 1200px; keep status, danger, info, and CTA values and the motion/spacing blocks unchanged <!-- agent: fullstack-engineer.build, depends_on: [], touches: [packages/ui/src/styles/tokens.css] -->
- [x] 4.2 Swap the heading font in the platform root layout from Manrope to Public Sans via `next/font` (weights 600 and 700, subsets latin and latin-ext), still exposing `--font-heading`; keep Inter and JetBrains Mono <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/src/app/layout.tsx] -->

## 5. Docs and verification

- [x] 5.1 Rewrite DESIGN.md to the new reality: token tables (colors, radii, container), typography section (Public Sans/Inter/JetBrains Mono), the icon section (Lucide with the card metadata mapping), component notes for the results bar, sidebar filters, always-visible map, and LotCard v2, and record the deliberate mockup deviations (status phases, mono numerals, m³ units) <!-- agent: fullstack-engineer.fast, depends_on: [3.3, 4.1, 4.2], touches: [DESIGN.md] -->
- [x] 5.2 Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build`; update markup-asserting tests that the theme and layout work moved (PortalHeader, ListingFilters suites are the likely ones); fix any contrast or a11y findings on the listing page; report results <!-- agent: fullstack-engineer.fast, depends_on: [1.3, 2.5, 3.4, 5.1], touches: [] -->
