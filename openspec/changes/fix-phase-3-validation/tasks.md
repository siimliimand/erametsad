# Tasks: fix-phase-3-validation

## 1. P0 — authorization and privacy

- [x] 1.1 Bind contracts to their owner: `prepareContract` stores the preparing user in `signedBy` at prepare time (no migration); `signContract` rejects a mismatched signer; the auction-type prepare route enforces the winner gate (403 without the `won` bid); `contract-state.ts` `prepared`/`sent` lookup filters by `signedBy` <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/src/lib/contracts/service.ts, apps/platform/src/app/api/v1/bids/contract/**, apps/platform/src/app/api/v1/bids/framework-contract/**, apps/platform/src/app/(portal)/lepingud/_components/contract-state.ts] -->
- [ ] 1.2 Contract binding tests: service tests (owner stored at prepare, cross-user sign rejected, 15-min expiry voids, hash recorded) and route tests (401 unauthed, 403 non-winner, cross-user sign rejected, user-scoped in-progress lookup) <!-- agent: fullstack-engineer.build, depends_on: [1.1], touches: [apps/platform/src/lib/contracts/__tests__/**, apps/platform/src/app/api/v1/bids/contract/**/__tests__/**, apps/platform/src/app/api/v1/bids/framework-contract/**/__tests__/**] -->
- [x] 1.3 Strip `amount` from public `bid:created` SSE frames (`auctionId` + `placedAt` only); `BidList` stops reading `payload.amount` and relies on its quiet refetch for authed amounts; update the realtime stream tests <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/src/do/auction.ts, apps/platform/src/lib/realtime/__tests__/**, apps/platform/src/app/(portal)/oksjon/[id]/_components/BidList.tsx] -->
- [x] 1.4 Correct docs/tasks.md Phase 3 claims: line 267 note the listing redesign superseded the grid/map toggle (map+list stacked by design); line 277 reword signed URLs to the Phase 2 media deferral; line 307 view counts and line 310 fee display stated as gaps <!-- agent: fullstack-engineer.fast, depends_on: [], touches: [docs/tasks.md] -->

## 2. P1 — spec conflicts

- [x] 2.1 Alapakkumine approve returns coded `higher_bid_exists` (HTTP 409) when the current leading regular bid exceeds the under-start amount; pending bid stays `pending_approval`; wire the drawer conflict panel to this code; update alapakkumine tests (409 case, clean approve still promotes) <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/src/lib/bidding/alapakkumine.ts, apps/platform/src/lib/bidding/__tests__/alapakkumine.test.ts, apps/platform/src/app/api/v1/my-auctions/[id]/underbids/[bidId]/approve/route.ts, apps/platform/src/app/(portal)/user/objects/_components/lot-drawer.tsx] -->
- [x] 2.2 Persist pending-alapakkumine state: viewer snapshot gains `hasPendingUnderStart` (own `pending_approval` bid on the auction); `BidPanel` derives the pending chip from `serverFlag || sessionState` <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/src/app/(portal)/oksjon/[id]/page.tsx, apps/platform/src/app/(portal)/oksjon/[id]/_components/BidPanel.tsx, apps/platform/src/lib/auction/queries.ts] -->

## 3. P2 — spec completeness

- [x] 3.1 Wire the shell quick search: parse `q` in `parseAuctionSearchParams`, apply title/cadastre matching in `listAuctions`, include in "Tühjenda" clear and active-count badge <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/src/lib/auction/queries.ts, apps/platform/src/app/(portal)/page.tsx, apps/platform/src/app/(portal)/_components/ListingFilters.tsx] -->
- [ ] 3.2 Contracts list shows the caller's `prepared`/`sent` rows with status pills and a resume action into the signing flow (user-scoped per 1.1) <!-- agent: fullstack-engineer.build, depends_on: [1.1], touches: [apps/platform/src/app/(portal)/lepingud/page.tsx, apps/platform/src/app/(portal)/lepingud/_components/contract-api.ts] -->
- [ ] 3.3 Copy alignment: fee notice "Teenustasu rakendub vaid oksjoni võitmise korral" in BidPanel + BidConfirmModal; no-rights message points to `info@erametsad.ee`; species tooltip map extended to all 24 codes and added to the dossier row <!-- agent: fullstack-engineer.fast, depends_on: [2.2], touches: [apps/platform/src/app/(portal)/oksjon/[id]/_components/BidPanel.tsx, apps/platform/src/app/(portal)/oksjon/[id]/_components/BidConfirmModal.tsx, apps/platform/src/app/(portal)/oksjon/[id]/_components/DossierTable.tsx] -->

## 4. P3 — hardening and tests

- [ ] 4.1 Switch `my/notifications` routes, `my/stream`, and underbids approve/reject to `resolveAccessTokenSession` (revoked session → 401); add one regression test for the revoked-token path <!-- agent: fullstack-engineer.build, depends_on: [2.1], touches: [apps/platform/src/app/api/v1/my/**, apps/platform/src/app/api/v1/my-auctions/[id]/underbids/**] -->
- [x] 4.2 Auth route tests: login (429 rate limit, isikukood lookup branch) and reset-password (policy enforcement, session revocation) <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/src/app/api/v1/auth/login/__tests__/**, apps/platform/src/app/api/v1/auth/reset-password/__tests__/**] -->
- [ ] 4.3 Component tests: SealedBidPanel state machine (locked card, winner/loser/unsold, revision-cap lock), BidList role shaping + outbid derivation + refetch on `bid:created`, LiveListing (prepend, ended flip) <!-- agent: fullstack-engineer.build, depends_on: [1.3], touches: [apps/platform/src/app/(portal)/oksjon/[id]/_components/__tests__/**, apps/platform/src/app/(portal)/_components/__tests__/**] -->
- [x] 4.4 Final verification: `pnpm lint`, `pnpm typecheck`, full vitest run, `pnpm build` <!-- agent: fullstack-engineer.fast, depends_on: [1.1, 1.2, 1.3, 1.4, 2.1, 2.2, 3.1, 3.2, 3.3, 4.1, 4.2, 4.3], touches: [] -->
