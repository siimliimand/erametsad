# Tasks: admin-auctions-demo-restyle

## 1. Shared foundations

- [ ] 1.1 Upgrade `DataTable` with the demo card treatment (white surface, 8px radius, card shadow via `--shadow-card`, edge-cell padding) and add optional sortable-column support: per-column sort descriptor (key, current direction, toggle href) rendering the demo `th-sort` button look with asc/desc arrow icons and `aria-sort` on the active `th`; extend the shared icon set with the sort arrows and column icons (tree-pine, map-pin-house, wheat, package, zap, external-link) needed by the auctions rows <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/src/app/(admin)/_components/DataTable.tsx, apps/platform/src/app/(admin)/_components/icons.tsx] -->
- [ ] 1.2 Add the demo animation keyframes to the admin token scope: `cd-blink` (countdown critical blink) and the `hover-none` touch fallback for row actions <!-- agent: fullstack-engineer.fast, depends_on: [], touches: [apps/platform/src/app/(admin)/admin.css] -->

## 2. List-view helpers

- [ ] 2.1 Extract the list-view helpers from the auctions page into `auctions/_lib/list-view.ts`: sort-key whitelist and comparator extended to id, title, minBidCents, bidCount, endsAt, createdAt (defaults unchanged: endsAt ascending when exactly the active status is filtered, otherwise -createdAt), countdown text, and initials; cover them with vitest tests <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/src/app/(admin)/admin/auctions/_lib/list-view.ts, apps/platform/src/app/(admin)/admin/auctions/_lib/list-view.test.ts] -->

## 3. Auctions page restyle (server)

- [ ] 3.1 Restyle the page head to the demo: breadcrumb line (Töölaud / Oksjonid), title row with "Uus oksjon" primary button carrying a ⌘N kbd hint, "Ekspordi CSV" ghost action moved up from the toolbar; restyle the type tabs as pill buttons with count badges using the pressed state on the active tab <!-- agent: fullstack-engineer.build, depends_on: [1.1], touches: [apps/platform/src/app/(admin)/admin/auctions/page.tsx] -->
- [ ] 3.2 Restyle the filter form as the demo chip filter bar: chip-wrapped selects (Olek keeps multi-select), inline date-range chip (Lõpp alates—kuni), pill search input, and the "Tühjenda (n)" active-filter count action; keep the GET submit and all URL parameter names <!-- agent: fullstack-engineer.build, depends_on: [3.1], touches: [apps/platform/src/app/(admin)/admin/auctions/page.tsx] -->
- [ ] 3.3 Wire working sort links into the `DataTable` headers (ID, Nimi, Alghind, Pakkumisi, Lõpp) via the extracted helpers, and restyle row presentation: type chip with icon + A/S letter badge and full-label tooltip, zap marker on quick auctions, specialist initials avatar with sr-only name, amber (p) pending-bid marker, hover-revealed row actions (Vaata portal link, Muuda, Dupl., end/archive/re-list controls restyled to the demo `ra-btn` look) <!-- agent: fullstack-engineer.build, depends_on: [2.1, 3.2], touches: [apps/platform/src/app/(admin)/admin/auctions/page.tsx] -->

## 4. Interactive components

- [ ] 4.1 Create the `Countdown` client component (ticking remaining time for active auctions, `cd-blink` critical class under five minutes, "lõppenud" at zero, `aria-label` with the full end time, server-rendered initial value as children to avoid layout shift) and use it in the Lõpp column <!-- agent: fullstack-engineer.build, depends_on: [1.2, 2.1], touches: [apps/platform/src/app/(admin)/admin/auctions/_components/Countdown.tsx, apps/platform/src/app/(admin)/admin/auctions/page.tsx] -->
- [ ] 4.2 Create the `AuctionsTable` client wrapper receiving serialized rows plus the server actions as props: row checkbox selection state, fixed dark bulk bar (Valitud N, "Ajasta avaldimine" with the datetime fields, "Ekspordi valitud", Tühista) shown only with a selection, selected-row highlight, hover/focus-revealed row actions with the `hover:none` fallback, and the ⌘N shortcut to /admin/auctions/new; preserve the server-action form contracts (ids + startsAt/endsAt; reason + outcome) <!-- agent: fullstack-engineer.build, depends_on: [3.3, 4.1], touches: [apps/platform/src/app/(admin)/admin/auctions/_components/AuctionsTable.tsx, apps/platform/src/app/(admin)/admin/auctions/page.tsx] -->
- [ ] 4.3 Create the `EndAuctionModal` client component on the native `<dialog>` element (showModal focus trap + Escape): irreversibility warning, auction context line from row data, outcome radio rows (winner / unsold), required reason minLength 5 with the demo hint, danger confirm button wrapping `endAuctionManuallyAction`; mount it from the row actions in `AuctionsTable` <!-- agent: fullstack-engineer.build, depends_on: [4.2], touches: [apps/platform/src/app/(admin)/admin/auctions/_components/EndAuctionModal.tsx, apps/platform/src/app/(admin)/admin/auctions/_components/AuctionsTable.tsx] -->

## 5. Verification

- [ ] 5.1 Visual regression against the demo (browser screenshots of /admin/auctions vs docs/design/demo/admin/02-auctions-list.html at desktop and mobile widths, plus spot-check two other admin screens that consume `DataTable`) and run lint, typecheck, build, and vitest across the workspace, fixing findings <!-- agent: fullstack-engineer.fast, depends_on: [4.3, 2.1], touches: [] -->
