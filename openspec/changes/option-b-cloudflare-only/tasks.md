## 1. Phase 0: decision spikes and foundations (go/no-go gate)

- [x] 1.1 Spike: Drizzle ORM on D1 locally via `wrangler d1` miniflare with a users/auctions/bids schema subset; document CRUD and `batch()` atomicity semantics in a spike report <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/spikes/d1-drizzle/**] -->
- [x] 1.2 Spike: minimal Durable Object (counter + WebSocket echo) inside the OpenNext worker; confirm `getCloudflareContext()` access; set up `@cloudflare/vitest-pool-workers` <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/wrangler.jsonc, apps/platform/spikes/do/**, apps/platform/vitest.config.ts] -->
- [x] 1.3 Spike: enable Cloudflare Email Service on the account, verify the `erametsad.ee` zone and sender, send to a verified destination, and record beta terms plus current daily quota <!-- agent: fullstack-engineer.build, depends_on: [], touches: [apps/platform/spikes/email/**] -->
  - COMPLETE WITH PRECONDITIONS (2026-08-28): report at `apps/platform/spikes/email/REPORT.md`. Rescoped to prototype zone `ww0.dev` (id 8761a52640daef70b6cf6f14d38e6dd9), sender `noreply@erametsad.ww0.dev`. Beta terms documented (3,000/month included, daily ramp, verified-destination sends exempt). Enable + test send NOT run: API token lacks `Email Sending : Edit` + `Email Routing : Edit` (+ `DNS : Read` on ww0.dev). Precondition folded into task 4.2; does not block Phases 1-2.
- [x] 1.4 Restore the provisioned KV namespace id `5b67cd2c595f4d31b3b1be5db76e9bef` in `wrangler.jsonc` (replacing the placeholder) <!-- agent: fullstack-engineer.fast, depends_on: [], touches: [apps/platform/wrangler.jsonc] -->
- [x] 1.5 Decision record: sign off the Payload replacement scope (full replacement), the integer-cents money rule, and answers to the five open questions (drafts/versioning usage, launch email volume, production data existence, admin user inventory, PDF generation placement) <!-- agent: fullstack-engineer.fast, depends_on: [1.1, 1.2, 1.3], touches: [docs/decisions/**] -->
- [x] 1.6 Phase 0 gate review: verify exit criteria (spikes green, admin scope signed off, launch email volume under the initial daily quota or a warm-up plan); record the go/no-go decision <!-- agent: fullstack-engineer.fast, depends_on: [1.4, 1.5], touches: [docs/decisions/**] -->

## 2. Phase 1: data layer on D1

- [x] 2.1 Inventory Payload local-API call sites and collection access rules (`payload/collections/*.access.ts`); define the repository surface (find/findByID/create/update/delete plus the `where` operators actually used) <!-- agent: fullstack-engineer.fast, depends_on: [1.6], touches: [apps/platform/src/lib/data/INVENTORY.md] -->
- [x] 2.2 Drizzle schema for the 14 core collections (users, profiles, company-access-requests, auction-rights, auctions, auction-subscriptions, bids, auto-bidders, contracts, contract-templates, notifications, audit-entries, leads, settings) in SQLite dialect with the fixed mapping rules <!-- agent: fullstack-engineer.build, depends_on: [1.1, 1.6], touches: [apps/platform/src/lib/data/schema/**] -->
- [x] 2.3 Drizzle schema for the 12 content collections (articles, pages, faq-categories, faq-items, testimonials, partner-services, legal-documents, redirects, specialists, statistics-snapshots, counties, parishes, media) in SQLite dialect <!-- agent: fullstack-engineer.build, depends_on: [1.6, 2.1], touches: [apps/platform/src/lib/data/schema/**] -->
  - Note: the non-core set in `src/payload/collections/` is exactly the 13 tables built. `notification-preferences` is not a real collection (try/catch call with defaults fallback in `lib/notifications/service.ts`) — fallback kept, behavior preserved. Optional cross-collection FKs (auctions.county_id etc.) documented but not added.
- [x] 2.4 Schema lint: fail the build on `REAL` money columns and on enum-like TEXT columns without CHECK constraints <!-- agent: fullstack-engineer.build, depends_on: [2.2, 2.3], touches: [apps/platform/scripts/lint-schema.ts] -->
- [x] 2.5 Repository layer for core collections: Payload-like surface from the inventory, TEXT-JSON parsing, cents conversion at the API boundary <!-- agent: fullstack-engineer.build, depends_on: [2.1, 2.2], touches: [apps/platform/src/lib/data/repositories/**] -->
- [x] 2.6 Repository layer for content collections <!-- agent: fullstack-engineer.build, depends_on: [2.1, 2.3], touches: [apps/platform/src/lib/data/repositories/**] -->
- [x] 2.7 Access-control guards: encode the inventoried Payload access rules as explicit guard functions enforced server-side in the repository layer <!-- agent: fullstack-engineer.build, depends_on: [2.5], touches: [apps/platform/src/lib/data/guards.ts] -->
- [x] 2.8 D1 binding and migrations flow: add the `DB` binding, remove the `NEON_DATABASE_URL`/`DATABASE_URL` plain-text bindings, wire `drizzle-kit generate` to `wrangler d1 migrations apply` for local and remote <!-- agent: fullstack-engineer.build, depends_on: [2.2, 2.3], touches: [apps/platform/wrangler.jsonc, apps/platform/drizzle.config.ts, apps/platform/package.json] -->
  - Note: remote D1 creation blocked by token perms (user action: `pnpm exec wrangler d1 create eametsad-db` from apps/platform, then replace the placeholder database_id in wrangler.jsonc). Local flow fully working.
- [x] 2.9 Port `src/lib/db.ts` and remaining direct SQL to the SQLite dialect; delete `withAuctionLock` (its job moves to AuctionDO in section 3) <!-- agent: fullstack-engineer.build, depends_on: [2.5, 2.8], touches: [apps/platform/src/lib/db.ts] -->
- [x] 2.10 Port `src/payload/seed/*.ts` to the new repositories so `pnpm seed:reset` reproduces the current fixtures on D1 <!-- agent: fullstack-engineer.build, depends_on: [2.5, 2.6, 2.8], touches: [apps/platform/src/payload/seed/**, apps/platform/src/lib/data/seed/**] -->
- [x] 2.11 Switch app routes, services, and workers from the Payload local API to the repositories with identical behavior <!-- agent: fullstack-engineer.build, depends_on: [2.5, 2.6, 2.7], touches: [apps/platform/src/app/**, apps/platform/src/lib/**] -->
  - Note: guard contexts default to the trusted system caller to preserve Payload `overrideAccess` behavior exactly; only the auto-bidders route wires a user guard. Phase 6 admin screens must wire per-route user guards.
- [x] 2.12 Remove the Payload bootstrap from the request path (payload config, instrumentation wiring, `@payloadcms/*` dependencies); the admin panel is absent until section 7 replaces it <!-- agent: fullstack-engineer.build, depends_on: [2.10, 2.11], touches: [apps/platform/src/payload/payload.config.ts, apps/platform/src/instrumentation.ts, apps/platform/package.json] -->
  - Note: kept `src/payload/seed/reset.ts` (delegator), `src/payload/access/roles.ts` (payload-free, live importers), `src/payload/collections/Users.ts` (rewritten as the isikukood hook module). `PAYLOAD_SECRET` still serves as the IP-hash salt — rename candidate for task 5.2/8.7.
- [x] 2.13 Phase 1 exit verification: all non-admin app routes run against local D1, seed reproduces fixtures, repository vitest suite passes, `pnpm lint`/`typecheck`/`build` green <!-- agent: fullstack-engineer.fast, depends_on: [2.4, 2.9, 2.11, 2.12], touches: [] -->
  - Evidence: `apps/platform/PHASE1-EXIT.md`. `/` `/api/health` `/api/v1/statistics` `/api/v1/auctions/stream` (SSE) verified 200 on the built worker with local D1. `/api/v1/auctions` and `/api/v1/faq` do not exist as routes (404 by design, not regression).

## 3. Phase 2: Durable Objects for auctions and rate limiting

- [x] 3.1 `AuctionDO` skeleton (`src/do/auction.ts`) with on-demand hydration: first touch loads auction state from D1; DO storage holds only hot state (current price, endsAt, subscriber set) <!-- agent: fullstack-engineer.build, depends_on: [1.2, 2.8], touches: [apps/platform/src/do/auction.ts] -->
- [x] 3.2 Bid admission in `AuctionDO`: port the place-bid validation chain (user status, amount vs current price, step, alapakkumine rules, sealed constraints, contract gate, rights), idempotency key replay, and one `batch()` write on accept (bid row + auction update + audit entry); autobidder evaluation runs in the DO <!-- agent: fullstack-engineer.build, depends_on: [2.9, 3.1], touches: [apps/platform/src/do/auction.ts, apps/platform/src/lib/bidding/place-bid.ts] -->
- [x] 3.3 Event hub in `AuctionDO`: `subscribe()` endpoint (SSE or WebSocket hibernation), subscriber registry, fan-out of `bid:created`, `auction:extended`, `auction:ended`, `auction:published` <!-- agent: fullstack-engineer.build, depends_on: [3.1], touches: [apps/platform/src/do/auction.ts] -->
- [x] 3.4 `alarm()` scheduling in `AuctionDO`: anti-snipe window check, auction end transition, winner computation including sealed-bid opening trigger, notification enqueue <!-- agent: fullstack-engineer.build, depends_on: [3.2], touches: [apps/platform/src/do/auction.ts] -->
- [x] 3.5 Rebuild `src/lib/realtime/auction-stream.ts` and `my-stream.ts` on top of AuctionDO events; keep the public event names so the frontend stays untouched <!-- agent: fullstack-engineer.build, depends_on: [3.3], touches: [apps/platform/src/lib/realtime/auction-stream.ts, apps/platform/src/lib/realtime/my-stream.ts] -->
- [x] 3.6 `RateLimiterDO` (`src/do/rate-limiter.ts`): token bucket per key; `src/lib/rate-limit.ts` keeps its API and delegates; wire the leads and auth endpoints to it <!-- agent: fullstack-engineer.build, depends_on: [3.1], touches: [apps/platform/src/do/rate-limiter.ts, apps/platform/src/lib/rate-limit.ts] -->
- [x] 3.7 `wrangler.jsonc` DO wiring: `durable_objects.bindings` for both classes plus the `migrations` list with the new SQLite-backed classes <!-- agent: fullstack-engineer.build, depends_on: [3.1, 3.6], touches: [apps/platform/wrangler.jsonc] -->
- [x] 3.8 Concurrency and broadcast tests via `@cloudflare/vitest-pool-workers`: N parallel bids on one auction yield one consistent winner-increment sequence and consistent D1 state; two clients in different simulacra both receive `bid:created` <!-- agent: fullstack-engineer.build, depends_on: [3.2, 3.4, 3.7], touches: [apps/platform/src/do/__tests__/auction.test.ts] -->
  - Note: exposed and fixed a real race — D1 binding calls do not close the DO input gate, so parallel admissions interleaved; admission now serialized behind a promise-queue mutex in `src/do/auction.ts`.

## 4. Phase 3: Email Service

- [x] 4.1 `src/lib/notifications/email-sender.ts` transport chain (EMAIL binding, then REST API, then SMTP) with `CLOUDFLARE_EMAIL_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` env; `next dev` keeps Mailpit <!-- agent: fullstack-engineer.build, depends_on: [1.3, 1.6], touches: [apps/platform/src/lib/notifications/email-sender.ts] -->
- [ ] 4.2 `send_email` binding in `wrangler.jsonc`; verify sending domain `erametsad.ee` and sender `noreply@erametsad.ee` <!-- agent: fullstack-engineer.build, depends_on: [4.1], touches: [apps/platform/wrangler.jsonc] -->
- [x] 4.3 Surface `error.code` (`E_RATE_LIMIT_EXCEEDED`, `E_DAILY_LIMIT_EXCEEDED`, and others) in notification logs; record per-recipient result (`delivered`/`queued`/`permanent_bounces`) on the notifications rows <!-- agent: fullstack-engineer.build, depends_on: [2.5, 4.1], touches: [apps/platform/src/lib/notifications/service.ts, apps/platform/src/lib/data/schema/**] -->
- [x] 4.4 GDPR: `List-Unsubscribe` headers on marketing templates; review transactional templates for the same <!-- agent: fullstack-engineer.build, depends_on: [4.1], touches: [packages/emails/src/templates/**] -->

## 5. Phase 4: auth and sessions on Workers

- [x] 5.1 Replace the in-memory `accessTokenSessions` map with a D1-backed session store; verify refresh-token families persist and rotation survives isolate restarts <!-- agent: fullstack-engineer.build, depends_on: [1.6, 2.5], touches: [apps/platform/src/lib/auth/session.ts] -->
- [x] 5.2 Port Node `crypto` to Web Crypto (`crypto.subtle`): `createHash`, `randomUUID`, HMAC in `jwt.ts`, `computeIpHash` in the bid path, sealed-bid encryption; keep a dual implementation only where local vitest needs it <!-- agent: fullstack-engineer.build, depends_on: [1.6], touches: [apps/platform/src/lib/auth/jwt.ts, apps/platform/src/lib/bidding/place-bid.ts, apps/platform/src/lib/encryption.ts] -->
- [x] 5.3 Audit `src/lib/auth/eid-provider.ts` for TCP/Node assumptions; test the flows against the real provider sandbox; document the port <!-- agent: fullstack-engineer.build, depends_on: [5.2], touches: [apps/platform/src/lib/auth/eid-provider.ts] -->
- [x] 5.4 Phase 4 exit verification: login, bid, logout cycle on the deployed worker; token-family rotation survives isolate restarts <!-- agent: fullstack-engineer.fast, depends_on: [3.2, 5.1, 5.2, 5.3], touches: [] -->

## 6. Phase 5: background jobs and queue

- [x] 6.1 Queue consumer worker: notification fan-out (one message per user and channel, idempotent via `dedupeKey`), email sending, contract PDF generation into R2 <!-- agent: fullstack-engineer.build, depends_on: [2.11, 4.1], touches: [apps/platform/src/workers/queue-consumer.ts] -->
- [x] 6.2 Replace the polling in `src/lib/workers/auction-ending.ts` with AuctionDO alarms plus a cron trigger sweep (`scheduled()` handler) as the safety net for evicted DOs <!-- agent: fullstack-engineer.build, depends_on: [1.6, 3.4], touches: [apps/platform/src/lib/workers/auction-ending.ts, apps/platform/wrangler.jsonc] -->
- [x] 6.3 Dead-letter queue with retry policy and alerting on DLQ depth <!-- agent: fullstack-engineer.build, depends_on: [6.1], touches: [apps/platform/wrangler.jsonc, apps/platform/src/workers/**] -->

## 7. Phase 6: admin UI (Payload replacement)

- [x] 7.1 Admin needs inventory: per collection, who edits it and which fields matter day-to-day; feeds the screen scope <!-- agent: fullstack-engineer.fast, depends_on: [1.6], touches: [apps/platform/src/app/(admin)/INVENTORY.md] -->
- [x] 7.2 Admin shell: `(admin)` route group with role guard on `users.role`, layout, navigation, Estonian labels, and table/form primitives on the repository layer with server actions <!-- agent: fullstack-engineer.build, depends_on: [2.11, 7.1], touches: [apps/platform/src/app/(admin)/**] -->
- [x] 7.3 Auction operations screens: create/publish auction, live bid monitor (subscribes to the same AuctionDO stream), approve/reject bids, sealed ceremony screens, contract flow trigger <!-- agent: fullstack-engineer.build, depends_on: [3.3, 7.2], touches: [apps/platform/src/app/(admin)/auctions/**] -->
- [x] 7.4 Users, rights, contracts, and leads CRM screens <!-- agent: fullstack-engineer.build, depends_on: [7.2], touches: [apps/platform/src/app/(admin)/users/**, apps/platform/src/app/(admin)/contracts/**, apps/platform/src/app/(admin)/leads/**] -->
- [x] 7.5 Content screens: articles, pages, FAQ categories and items, testimonials, partner services, legal documents, redirects, specialists, statistics snapshots, settings <!-- agent: fullstack-engineer.build, depends_on: [7.2], touches: [apps/platform/src/app/(admin)/content/**] -->
- [x] 7.6 Media library on R2: upload, browse, alt text; replaces the Payload Media UI <!-- agent: fullstack-engineer.build, depends_on: [7.2], touches: [apps/platform/src/app/(admin)/media/**] -->
- [x] 7.7 Content import/export tooling (JSON) so marketing can bulk-load articles and pages <!-- agent: fullstack-engineer.build, depends_on: [7.5], touches: [apps/platform/src/app/(admin)/content/import-export/**] -->

## 8. Phase 7: testing, migration, and cutover

- [x] 8.1 Adapt the vitest suites: repository tests against `better-sqlite3` in memory with the same Drizzle schema; DO tests via `@cloudflare/vitest-pool-workers`; port the existing bidding tests to exercise AuctionDO admission <!-- agent: fullstack-engineer.build, depends_on: [2.11, 3.8], touches: [apps/platform/vitest.config.ts, apps/platform/src/lib/bidding/__tests__/**, apps/platform/src/do/__tests__/**] -->
- [x] 8.2 Property tests for money handling: cents integers survive the API boundary in both directions; no `REAL` drift <!-- agent: fullstack-engineer.build, depends_on: [2.4, 8.1], touches: [apps/platform/src/lib/data/__tests__/**] -->
  - Fixed two real bugs in `eurosToCents`/`centsToEuros`: unsafe-integer inputs now rejected above a documented MAX_CENTS bound.
- [x] 8.3 Load test bid bursts at auction close against the deployed worker; validate the D1 single-writer ceiling and DO serialization <!-- agent: fullstack-engineer.build, depends_on: [3.7, 8.1], touches: [apps/platform/scripts/loadtest/**] -->
  - Local numbers (wrangler dev): DO serialized admission ceiling ~16-19/s; 116 accepted bids across runs → 116 D1 rows, 1:1 audit, exactly one leader. 500s were miniflare proxy artifacts — re-verify on real deployment.
- [x] 8.4 Data migration path: export Postgres, transform per the mapping rules, import D1; if no production data exists (expected), document the fresh-seed cutover <!-- agent: fullstack-engineer.build, depends_on: [8.1], touches: [apps/platform/scripts/migrate-pg-to-d1/**] -->
- [x] 8.5 Cutover runbook: DNS, secrets via `wrangler secret put`, queue consumers enabled, rollback via the previous Worker version <!-- agent: fullstack-engineer.fast, depends_on: [8.3, 8.4], touches: [docs/runbooks/cutover-cloudflare-only.md] -->
- [ ] 8.6 End-to-end verification on production: publish, bid, anti-snipe, end, win, contract, email all green <!-- agent: fullstack-engineer.fast, depends_on: [4.2, 6.3, 7.3, 8.5], touches: [] -->
- [ ] 8.7 Update `ARCHITECTURE.md`, `DESIGN.md` where affected, and the project guardrails to describe the D1/DO/Email stack <!-- agent: fullstack-engineer.fast, depends_on: [8.6], touches: [ARCHITECTURE.md, .agents/skills/pc-guardrails-project/SKILL.md] -->
